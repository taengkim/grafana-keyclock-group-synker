# Example only — never commit real values. Create the real Secret out of band,
# e.g. with:
#   kubectl create secret generic grafana-team-sync \
#     --from-literal=KEYCLOAK_CLIENT_SECRET='...' \
#     --from-literal=GRAFANA_TOKEN='...'
# or manage it with SealedSecrets / ExternalSecrets.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-team-sync
type: Opaque
stringData:
  KEYCLOAK_CLIENT_SECRET: ""
  GRAFANA_TOKEN: ""
